global:
  scrape_interval: 60s
  scrape_timeout: 30s
  evaluation_interval: 60s

scrape_configs:
  - job_name: "pano-sase-prom-exporter"
    static_configs:
      - targets: ["exporter:9850"]
        labels:
          environment: "production"
          service: "prisma-sdwan"
